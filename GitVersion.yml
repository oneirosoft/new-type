strategies:
  - Mainline
branches:
  main:
    regex: ^main$
    source-branches: []
    is-main-branch: true
  topics:
    regex: ^topics?[/-](?<BranchName>.+)
    label: '{BranchName}'
    source-branches:
      - main
  pull-requests:
    tracks-release-branch: true


